{
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "args": [
                "run",
                "mwn",
                "--no-xds",
            ],
            "program": "${workspaceFolder}"
        }
    ]
}